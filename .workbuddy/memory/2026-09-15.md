# 2026-09-15

## 整理 few-shot 实验记录到 experiment.md

把散落在 `logs/`、`runs/`、`experiment/` 的 few-shot 实验结果汇总进根目录 `experiment.md`（原为空文件）。

梳理出的实验链（均为「固定支持集 + 跨数据集监督微调」，非 episodic 元学习）：

- E0 09-10 LoveDA 旧协议 pilot（每域 5-shot，v1 权重，best 44.84，已被取代）
- E1 09-12 LoveDA GUI 支持集导出（1599 张固定评估集，协议 8184bef3…）
- E2 09-12 LoveDA v2 主实验 adapter/2000，0/1/2/5/10-shot × 3 seeds → 53.34 / 49.33 / 51.59 / 53.30 / 55.36
- E3 09-12 LoveDA 单种子消融 → 1/2-shot 上 semantic-head/200 最优（53.59 / 53.53）
- E4 09-13 SUIM GUI 手动集（78 张）**未被采用**，服务器用自动协议（79 张）
- E5 09-14 LoveDA 10-shot semantic-head/200 → 53.59，比 adapter 低 1.88 ⇒ E3 结论不能外推到大 K
- E6/E7 09-14 SUIM 协议 + 16 次矩阵（4 setting × 4 shots × 1 seed），adapter/2000/10-shot 最佳 43.56
- E8 09-15 SUIM 诊断报告 `runs/suim_analysis_20260915/analysis.md`

核心结论：最优更新范围依赖 K（小 K 限制更新、大 K 用 adapter）；1-shot adapter 会低于 0-shot；预算与更新范围必须一起调；SUIM 弱类是 PF/RO。

项目约定（供后续沿用）：
- 指标统一用百分数记录；LoveDA 结果是 Val 内部划分，不能称官方 Val 指标。
- `fewshot_hyperseg/` 的 episodic 模型从未跑过实验。
- 已知坑：`run_manual.py` 曾不接受 `--modes semantic-head`；SUIM `run_grid.py` 需从项目根目录运行否则 `ModuleNotFoundError: No module named 'experiment'`；SUIM 各次运行的 `run_config.json` 未下载，lr/crop/batch 只能按代码默认值推断。
